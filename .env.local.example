# Copy to .env.local and fill in from the Firebase console:
# Project settings -> General -> Your apps -> Web app -> SDK setup and configuration
#
# These are NEXT_PUBLIC_ because the Firestore web SDK runs in the browser.
# They are not secrets; Firestore security rules are what actually protect data.

NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
